class AuthModel {

    req
    res
    connection

    constructor(req, res, connection){
        this.req = req
        this.res = res
        this.connection = connection
    }

    login(passord, senha) {

    }
}